{
    "queries": {
        "_internal": {
            "first_internal" : "select * from \"http\" where time >= now() -10m",
            "second_internal" : "select * from cq where time >= now() -5m"
        }
    }
}
